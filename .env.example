# Build-time settings for the GitHub Pages frontend (Vite reads VITE_* only).
# Base URL of the deployed order API (Cloudflare Worker), without trailing slash.
# Leave empty to build without ordering (the order page shows an estimate only).
VITE_ORDER_API_URL=https://typefab-orders.<your-subdomain>.workers.dev
